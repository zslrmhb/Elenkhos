# Website Components

This directory is reserved for reusable structural components generated or
maintained by `roadmap-website-builder`.

Components must consume `website/content-manifest.json`, preserve canonical
IDs, and provide keyboard-accessible behavior. Do not place hardcoded roadmap
datasets here. The generated website is currently absent pending upstream data
migration.
